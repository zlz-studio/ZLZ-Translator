# คู่มือสำหรับคนที่ได้รับโปรแกรมนี้ต่อ (ติดตั้งเองใน 10 นาที)

โปรแกรมแปลภาษาสำหรับคุยกับลูกค้าต่างชาติบน Discord และทุกแอปบน Windows
พิมพ์ไทยในช่องแชท กด **F9** ข้อความกลายเป็นอังกฤษ (ยังไม่ส่ง อ่านก่อนแล้วกด Enter เอง)
ลากคลุมข้อความอังกฤษ กด **F8** ได้คำแปลไทย

**ไฟล์นี้ไม่มีคีย์หรือรหัสใดๆ ของคนที่ส่งให้** คุณต้องใช้บัญชีของตัวเองทั้งหมด

## สิ่งที่ต้องมี

| อย่างน้อยต้องมี 1 อย่าง | ค่าใช้จ่าย | ใช้ทำอะไร |
|---|---|---|
| **บัญชี Google** (Gmail) | ฟรี | ขอคีย์ Gemini มาแปล เร็วราว 1 วินาที |
| บัญชี Claude แบบเสียเงิน (Pro/Max) | ตามแพ็กเกจที่มีอยู่แล้ว | แปลผ่าน Claude Code แม่นกว่า ช้ากว่า (4 ถึง 5 วินาที) |

ถ้ามีแค่ Gmail ก็ใช้ได้ครบทุกอย่าง

## ขั้นตอน (แบบง่าย: ตัวติดตั้ง ไม่ต้องลง Python)

1. เปิด https://github.com/zlz-studio/ZLZ-Translator/releases โหลดไฟล์ **`ZLZ-Translator-Setup-x.y.z.exe`** ตัวล่าสุด
2. ดับเบิลคลิก กด **Next** จนเสร็จ (ไม่ต้องสิทธิ์ Admin)
3. โปรแกรมเปิดเองพร้อม **ตัวช่วยตั้งค่าทีละขั้น** ทำตามที่หน้าจอบอก:
   - ขั้น Claude Code: ถ้ามีบัญชี Claude Pro กด "ติดตั้งให้เลย" ถ้าไม่มีติ๊ก "ใช้ Gemini อย่างเดียว"
   - ขั้น Gemini: กดปุ่ม "เปิดหน้าขอคีย์" → กด Create API key ในเว็บ → ก๊อปมาวาง → กด "ตรวจสอบ"
   - ขั้น Login Claude (ถ้ามี Pro): กด "Login Claude" ยืนยันในเบราว์เซอร์ แล้วกลับมา
4. เสร็จแล้วโปรแกรมอยู่ที่ไอคอนสี่เหลี่ยมน้ำเงินมุมขวาล่างจอ ใช้ **F8 / F9** ได้เลย

ถ้าใช้วิธีนี้ **ข้ามหัวข้อด้านล่างได้ทั้งหมด** (ด้านล่างคือวิธีเดิมแบบรันจากซอร์สโค้ด)

## ขั้นตอน (แบบเดิม: รันจากซอร์สโค้ด)

**1. ติดตั้ง Python** (ถ้ายังไม่มี)
- ไปที่ python.org/downloads โหลด Python 3.12 หรือใหม่กว่า
- ตอนติดตั้ง **ติ๊ก "Add python.exe to PATH"** ก่อนกด Install

**2. โหลดโปรแกรม** เลือกทางใดทางหนึ่ง แล้ววางไว้ที่ไหนก็ได้ เช่น `Documents\Discord-Translator`
- *ง่ายสุด*: เปิด https://github.com/zlz-studio/ZLZ-Translator กดปุ่มเขียว **Code** แล้ว **Download ZIP** แตกไฟล์
- *ถ้ามี git*: `git clone https://github.com/zlz-studio/ZLZ-Translator.git` (ข้อดี: อัปเดตเวอร์ชันใหม่ได้ด้วยดับเบิลคลิก `update.bat`)

**3. ดับเบิลคลิก `setup.bat`** รอจนขึ้น "ติดตั้งเสร็จแล้ว" (ใช้เน็ตโหลดไลบรารีราว 1 ถึง 2 นาที)

**4. เปิดโปรแกรม** ดับเบิลคลิก **`run_hotkey.bat`** จะมีไอคอนสี่เหลี่ยมน้ำเงินที่มุมขวาล่างของจอ (system tray ถ้าไม่เห็นกดลูกศร ^)

**5. ใส่คีย์ Gemini (ไม่ต้องเปิดไฟล์ใดๆ)**
1. **คลิกขวาที่ไอคอน** เลือก **ตั้งค่าคีย์และบัญชี...**
2. กดปุ่ม **เปิดหน้าขอคีย์ Gemini** เบราว์เซอร์จะเปิด aistudio.google.com/apikey ล็อกอิน Gmail
3. กด **Create API key** ตั้งชื่ออะไรก็ได้ เลือกโปรเจกต์ **Default Gemini Project** แล้วกด **Copy key**
4. กลับมาที่หน้าต่างตั้งค่า วางคีย์ในช่อง `GEMINI_API_KEY`
5. กด **บันทึกแล้วทดสอบแปล** ถ้าขึ้นข้อความสีเขียว "ใช้ได้" แปลว่าเสร็จแล้ว

**6. ทดสอบ** เปิด Discord คลิกในช่องพิมพ์ พิมพ์ไทย กด **F9** รอ 1 ถึง 2 วินาที ข้อความจะกลายเป็นอังกฤษ พร้อมหน้าต่างเล็กบอกว่า "ลูกค้าจะอ่านว่า" อะไร อ่านแล้วค่อยกด Enter

**7. (แนะนำ) ให้เปิดเองตอนเข้า Windows** คลิกขวาไอคอน ติ๊ก **เปิดอัตโนมัติเมื่อเข้า Windows**

## ไอคอนที่มุมขวาล่าง

- **คลิกซ้าย** = เปิด/ปิดการทำงานชั่วคราว (ไอคอนสีเทาคือปิดอยู่ ปุ่ม F8/F9 จะไม่ทำงาน)
- **คลิกขวา** = เมนู: ตั้งค่าคีย์, น้ำเสียงตอนตอบ, Discord app, เปิดอัตโนมัติ, คลังศัพท์, ออกจากโปรแกรม
- สีส้ม = กำลังแปล

## ปุ่มลัดทั้งหมด

| ปุ่ม | ทำอะไร |
|---|---|
| **F9** | ไทยในช่องพิมพ์ -> อังกฤษ (ยังไม่ส่ง) |
| **F8** | ลากคลุมอังกฤษ -> ไทย |
| **Shift+F8** | ลากคลุมอังกฤษ -> อธิบายว่าเขาต้องการอะไร น้ำเสียงยังไง |
| **Shift+F9** | อังกฤษที่พิมพ์เอง -> แก้ให้ถูก |

ปุ่มลัดใช้ได้ **ทุกโปรแกรม** (Discord, เบราว์เซอร์, อีเมล, LINE) ไม่ใช่แค่ Discord
**เปลี่ยนปุ่ม**: คลิกขวาไอคอน tray เลือก "ตั้งค่าคีย์และบัญชี..." หัวข้อ 4 กด "กดปุ่ม" แล้วกดคีย์ที่ต้องการ กดบันทึก
ถ้าใช้ Unity หรือ Visual Studio ที่ใช้ F8/F9 อยู่แล้ว ติ๊ก "ให้ปุ่มลัดทำงานเฉพาะตอนหน้าต่าง Discord เปิดอยู่" หรือคลิกซ้ายไอคอนเพื่อปิดชั่วคราว

## ทางเลือกเพิ่มเติม (ไม่บังคับ)

- **ใช้ Claude แทน Gemini** ถ้ามีแพ็กเกจ Claude Pro/Max: ติดตั้ง Claude Code จาก claude.com/claude-code แล้วรัน `claude auth login` ใน PowerShell จากนั้นใน `config.toml` แก้ `provider_order = ["claude_code", "gemini"]`
- **คลิกขวาแปลในตัว Discord และใช้บนมือถือ**: ต้องสร้าง Discord app ของตัวเอง ดูขั้นตอนใน `SETUP_GUIDE.md` ขั้นที่ 4 แล้วใส่ `DISCORD_TOKEN=` ใน `.env`
- **คลังศัพท์** เปิด `glossary.md` ใส่ชื่อโปรเจกต์ ศัพท์ที่ห้ามแปล และสไตล์ที่ต้องการ เขียนภาษาไทยได้เลย

## ข้อควรรู้

- คีย์ Gemini ฟรีของ Google ใช้ได้หลักร้อยครั้งต่อวัน ไม่มีทางโดนหักเงินถ้าไม่ได้เปิด Billing เอง
- ข้อความที่ส่งไปแปลผ่าน Gemini แพ็กเกจฟรี Google อาจนำไปใช้พัฒนาระบบ ไม่ควรส่งข้อมูลลับของลูกค้า
- ไฟล์ `.env` เป็นความลับ ห้ามส่งต่อให้ใคร ถ้าจะแจกโปรแกรมต่อ ให้ลบไฟล์ `.env` และโฟลเดอร์ `.venv`, `data` ออกก่อน
